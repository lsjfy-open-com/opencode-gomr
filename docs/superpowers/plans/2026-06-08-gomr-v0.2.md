# GOMR v0.2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build persistent memory, trace, snapshot, graph, and visual export support for GOMR v0.2.

**Architecture:** Extend the current single-runtime architecture instead of introducing a separate server or build system. Keep wrappers compatible and derive new artifacts from deterministic local files.

**Tech Stack:** TypeScript ESM, Node 24 built-in test runner, static HTML/CSS/JS.

---

### Task 1: Red Tests

**Files:**
- Modify: `.opencode/gomr/gomr.test.ts`

- [ ] Add tests for non-empty init, persistent traces, table ledger, snapshots, graph data, visual export, and plugin-generated snapshots.
- [ ] Run `node --no-warnings --test .opencode/gomr/gomr.test.ts` and confirm the new tests fail for missing v0.2 behavior.

### Task 2: Runtime Implementation

**Files:**
- Modify: `.opencode/gomr/runtime.ts`
- Modify: `.opencode/plugins/gomr.ts`

- [ ] Seed required memory, graph, path, trace, cache, and visual directories.
- [ ] Write frontmatter Markdown nodes without empty placeholders.
- [ ] Persist trace JSONL to `.orca-memory/traces/session-<id>.jsonl`.
- [ ] Update ledger tables and preserve compact action state.
- [ ] Generate context snapshots from context plans and traces.
- [ ] Build graph/timeline data and export static visualization.

### Task 3: Verification

**Files:**
- Modify: `README.md` if required by changed paths or commands.

- [ ] Run the focused Node test command until all tests pass.
- [ ] Inspect generated sample artifacts through test assertions.
- [ ] Report any intentionally deferred CLI command surface beyond existing wrappers.
